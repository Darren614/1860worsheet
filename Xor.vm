push argument 0    // Load x onto the stack
push argument 1    // Load y onto the stack
or                 // Compute x OR y

push argument 0    // Reload x
push argument 1    // Reload y
and                // Compute x AND y
not                // Negate the AND result to get NOT(x AND y)

and                // Compute (x OR y) AND NOT(x AND y) = x XOR y

pop argument 2     // Store result in argument[2]
push argument 2    // Leave result at the bottom of the stack

// Infinite loop to halt execution
(END)
@END
0;JMP